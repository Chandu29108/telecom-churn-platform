-r requirements.txt

pytest==8.3.2
httpx==0.27.2
# Starlette 1.x's TestClient prints a deprecation warning recommending
# `httpx2` instead of `httpx` for the test client transport. Not urgent —
# it's a warning, not a failure, and the whole suite passes as-is — but
# worth switching next time httpx-related deps get touched.

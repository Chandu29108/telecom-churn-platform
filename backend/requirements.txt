fastapi==0.115.0
uvicorn[standard]==0.30.6
pandas==2.2.2
numpy==1.26.4
scikit-learn==1.5.1
scipy==1.13.1
joblib==1.4.2
sqlalchemy==2.0.32
psycopg2-binary==2.9.9
python-multipart==0.0.9
pydantic==2.8.2
python-dotenv==1.0.1

# --- Added for v2: auth, explainability, copilot, hardening ---
python-jose[cryptography]==3.3.0   # JWT issuing/verification
passlib[bcrypt]==1.7.4             # password hashing
# Pinned explicitly: passlib 1.7.4's internal bcrypt self-test crashes
# against bcrypt>=4.1 ("password cannot be longer than 72 bytes") due to a
# known passlib/bcrypt compatibility bug (passlib is unmaintained upstream).
bcrypt==4.0.1
email-validator==2.2.0             # pydantic EmailStr support
shap==0.46.0                       # per-customer explainability (SHAP)
slowapi==0.1.9                     # rate limiting
httpx==0.27.2                      # calling the local Ollama server
alembic==1.13.2                    # DB schema migrations (see /alembic)
boto3==1.34.162                    # Cloudflare R2 (S3-compatible) model storage — see app/model_store.py.
                                    # Only used if R2_* env vars are set; local dev works without R2 configured.

# --- Added for Phase 2: observability ---
# sentry-sdk==2.14.0                # Uncomment + install to enable error tracking. Only initialized if
                                    # SENTRY_DSN is set (see app/config.py) — optional, not required to run.
